name: Version check

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  version-check:
    name: version-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Ensure .gs and .txt are byte-identical
        run: |
          if ! diff -q gmail-transaction-alerts-Code.gs gmail-transaction-alerts-Code.txt; then
            echo "::error::gmail-transaction-alerts-Code.gs and gmail-transaction-alerts-Code.txt must be byte-identical."
            echo "Regenerate with: cp gmail-transaction-alerts-Code.gs gmail-transaction-alerts-Code.txt"
            exit 1
          fi
          echo "Bundle copies are in sync."

      - name: Extract PR parserVersion from both files
        id: pr_version
        run: |
          GS=$(node .github/scripts/extract-parser-version.js gmail-transaction-alerts-Code.gs)
          TXT=$(node .github/scripts/extract-parser-version.js gmail-transaction-alerts-Code.txt)
          if [ "$GS" != "$TXT" ]; then
            echo "::error::parserVersion differs between .gs ($GS) and .txt ($TXT)"
            exit 1
          fi
          echo "version=$GS" >> "$GITHUB_OUTPUT"
          echo "PR parserVersion: $GS"

      - name: Detect script file changes vs main
        id: changes
        run: |
          git fetch origin main --depth=1
          if git diff --name-only origin/main...HEAD | grep -E '^gmail-transaction-alerts-Code\.(gs|txt)$' >/dev/null; then
            echo "script_changed=true" >> "$GITHUB_OUTPUT"
            echo "Script bundle files changed in this PR."
          else
            echo "script_changed=false" >> "$GITHUB_OUTPUT"
            echo "No .gs/.txt changes; version bump not required."
          fi

      - name: Extract main parserVersion
        id: main_version
        run: |
          MAIN_GS=$(git show origin/main:gmail-transaction-alerts-Code.gs)
          MAIN_VER=$(printf '%s' "$MAIN_GS" | node -e "
            const fs = require('fs');
            const source = fs.readFileSync(0, 'utf8');
            const m = source.match(/parserVersion:\\s*['\"]([0-9]+\\.[0-9]+\\.[0-9]+)['\"]/);
            if (!m) { console.error('No parserVersion on main'); process.exit(1); }
            process.stdout.write(m[1]);
          ")
          echo "version=$MAIN_VER" >> "$GITHUB_OUTPUT"
          echo "main parserVersion: $MAIN_VER"

      - name: Advisory bump suggestion
        if: steps.changes.outputs.script_changed == 'true'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_VER: ${{ steps.pr_version.outputs.version }}
          MAIN_VER: ${{ steps.main_version.outputs.version }}
        run: |
          CHANGED=$(git diff --name-only origin/main...HEAD)
          SUGGESTION="patch"
          REASON="default for script changes that look like fixes"

          if echo "$CHANGED" | grep -E '^(fixtures/|tests/)' >/dev/null; then
            if echo "$CHANGED" | grep -E 'gmail-transaction-alerts-Code\.(gs|txt)$' >/dev/null; then
              # New fixtures often mean new/changed parser coverage → minor
              if git diff origin/main...HEAD -- fixtures/ | grep -E '^\+' | grep -qiE 'debit|purchase|merchant|authorization'; then
                SUGGESTION="minor"
                REASON="fixtures/tests suggest new or expanded alert coverage"
              fi
            fi
          fi

          if git diff origin/main...HEAD -- gmail-transaction-alerts-Code.gs | grep -E '^\+.*function parse' >/dev/null; then
            SUGGESTION="minor"
            REASON="new parse* function(s) added"
          fi

          if git diff origin/main...HEAD -- gmail-transaction-alerts-Code.gs | grep -E "^\+.*TRANSACTION_HEADERS|^\-.*TRANSACTION_HEADERS|^\+.*'Imported At'|^\-.*'Parser Version'" >/dev/null; then
            SUGGESTION="major"
            REASON="sheet column/header contract may have changed"
          fi

          BODY=$(printf '%s\n' \
            "### Version bump advisory (non-blocking)" \
            "" \
            "- **main:** \`$MAIN_VER\`" \
            "- **this PR:** \`$PR_VER\`" \
            "- **suggested semver level:** \`$SUGGESTION\` ($REASON)" \
            "" \
            "Bump \`APP_CONFIG.parserVersion\` in \`.gs\`, then \`cp gmail-transaction-alerts-Code.gs gmail-transaction-alerts-Code.txt\`." \
            "This comment is advisory only; the required check is that the version differs from main when script files change." \
            "" \
            "<!-- version-bump-advisory -->")

          # Update or create a single sticky comment
          EXISTING=$(gh api "repos/${{ github.repository }}/issues/${PR_NUMBER}/comments" \
            --jq '.[] | select(.body | contains("<!-- version-bump-advisory -->")) | .id' | head -n1)
          if [ -n "$EXISTING" ]; then
            gh api -X PATCH "repos/${{ github.repository }}/issues/comments/${EXISTING}" \
              -f body="$BODY" >/dev/null
          else
            gh api -X POST "repos/${{ github.repository }}/issues/${PR_NUMBER}/comments" \
              -f body="$BODY" >/dev/null
          fi

      # Requires parserVersion to be strictly GREATER than main's, not merely
      # different. An equality check passes a branch carrying an older version,
      # which downgrades the release and mis-stamps the Parser Version audit
      # column. See .github/scripts/compare-parser-versions.js and
      # tests/version-compare.test.js.
      - name: Require version bump when script changes
        if: steps.changes.outputs.script_changed == 'true'
        env:
          PR_VER: ${{ steps.pr_version.outputs.version }}
          MAIN_VER: ${{ steps.main_version.outputs.version }}
        run: node .github/scripts/compare-parser-versions.js "$PR_VER" "$MAIN_VER"
